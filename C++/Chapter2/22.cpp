// Multiple conditions Using &&(and) and ||(or). 
//Take a Positive Integer inpur and Tell if it is a 3 digit number or not.
#include <iostream>

using namespace std;

int main()
{int x;
    cin>>x;
// if(x>99 and x<1000)
if (x>99 && x<1000) {
cout<<"the number is a Three Digit Number"<<endl;}
else{
cout<<"Not a 3 digit Number"<<endl;}

 
  return 0;
}
// in if condition and is used so both conditions must be true to execute the code. 
//and operator can also be used as by typing &&.
// logical operator (and,or,not,nor).
