// Updation Of variables:
#include <iostream>

using namespace std;

int main()
{
  int x=5;
  cout<<x<<endl;
  //cout<<"Hi"<<endl;
  // x=8;
  x += 2;// this means increment or add 2 in x.
  x=x+7;
  cout<<x<<endl;
  // x=x-20;
  x-=4
  cout<<x<<endl;
  // x=x*10;
  x*=2
  cout<<x<<endl;
  return 0;
}
/*the following process is known as over-writing or updation. as x value is updated from 5 to 8. 
we can change or update the variable value multiple times. 
in this "=" doesn't means equal to but it means assignment. this operator is used for assigning a value. 
negative values can also be printed. 

*/